#include <stdio.h>

int main() {
    int i = 0;
    
    // Loop: prints a message for each iteration until i reaches 10
    while (i < 10) {
        printf("Iteration %d\n", i);
        i++;
    }

    return 0;
}